<!-- Keep it short. Delete sections that don't apply. -->

## What & why

<!-- One or two sentences: what this changes and the reason. -->

## Changes

-

## Checks

- [ ] `backend`: `npx tsc --noEmit` passes
- [ ] `backend`: `npx jest --no-coverage` passes
- [ ] `frontend`: `npx tsc --noEmit` passes
- [ ] Prisma schema touched? migration committed in this PR (or comment-only → no migration)
- [ ] Rebased onto the latest `main` (no direct commits to `main`)

## Notes for the reviewer

<!-- Anything non-obvious: a design trade-off, a follow-up, a legal caveat, etc. -->
